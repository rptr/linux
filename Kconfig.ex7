menuconfig MODULES
        bool "Enable loadable module support"
        option modules
	default y

config A
	bool "A prompt"

config B
	bool "B prompt"
	select A

config C
	bool "C prompt"
#	depends on A && !B
