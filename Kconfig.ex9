menuconfig MODULES
        bool "Enable loadable module support"
        option modules
	default y

config A
	bool "A"

config B
	bool "B"

config C
	bool "C"
	depends on A || B

config D
	bool "D"
	depends on !A
